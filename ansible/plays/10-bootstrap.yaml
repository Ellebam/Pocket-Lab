- name: Bootstrap users
  hosts: genai_servers
  become: true
  gather_facts: true
  gather_subset: min
  remote_user: "{{ hostvars[inventory_hostname].conn_user | default(ansible_user) }}"

  roles:
  - l3d.users.user
  - l3d.users.admin
