# ---------- Docker Volumes & misc ----------------------------

*/*-data/
acme.json
ragflow/logs/

# ---------- Ansible inventory & vars ----------------------------
ansible/inventory/*.yml
!ansible/inventory/hosts_example.yml     # ship the example
ansible/inventory/group_vars/
ansible/inventory/host_vars/

# ---------- Environment / secrets -------------------------------
.env
*.env                                   # but keep the templates
